package com.focusblack.wallos.util

import android.app.Activity
import android.util.Log
import androidx.fragment.app.FragmentActivity

object ReviewHelper {
    private const val TAG = "ReviewHelper"

    fun showReviewIfAppropriate(activity: Activity) {
        // Very small stub — in production use Play Core Review API
        Log.i(TAG, "showReviewIfAppropriate: pretend to launch in-app review")
    }
}
